# 非技术人员 Vibe Coding 实战指南

## 这份文档是什么

这是一份面向不懂代码的人的 AI 协作开发指南。它基于 Vibe Coding 的实际痛点分析、以及"从想法到可运行产品"全流程的拆解，提炼出一套可操作的方法。

---

## 第一部分：理解你在做什么

### Vibe Coding 的定义

不懂代码的人，通过自然语言对话，让 AI 把想法变成可运行的软件。

核心特征：
- 你不写代码，AI 写
- 你不看代码，靠运行结果判断对不对
- 你通过对话推进项目
- 你的输入是"我想要什么"，不是"怎么实现"

### 你的角色：产品经理 + 测试经理

你不是程序员，AI 才是程序员。你的职责是：

**作为产品经理：**
- 定义要做什么（产品定义、PRD）
- 定义做成什么样（行为契约）
- 确认每个功能是否符合预期（验收）

**作为测试经理：**
- 告诉 AI 怎么算"做对了"（验证标准）
- 操作验证每个改动（手动测试）
- 发现问题时精确描述现象（bug 报告）
- 决定是否接受 AI 的修复（质量把关）

**核心控制力公式：精确的需求 + 分步的验证 + 及时的喊停 = 不需要看懂代码也能控制项目质量**

---

## 第二部分：为什么你现在的方式效果差

### 典型的失败流程

1. 让 AI 输出一份比较宽泛的 PRD
2. 让 AI 输出一份比较宽泛的实施方案
3. 直接让 AI 开始改代码
4. 出 bug 后继续在同一段对话里追着修

### 五个根本问题

| 问题 | 解释 |
|------|------|
| PRD 太模糊 | 功能描述级别（"用户可以登录"），不是行为契约级别（"输入错误密码时返回什么、错几次锁定"）。AI 读到模糊 PRD 会自己"填空"，填的方式你无法预判 |
| 一口气改太多 | AI 按照宏观实施方案一次改 10 个文件，你看不懂代码，无法判断哪个文件改对了、哪个改出了新问题 |
| 没有可操作的验证方式 | 你唯一能验证的方式是"在界面上大概试试"，太粗了，很多内部逻辑错误你看不到 |
| 修 bug 没有协议 | AI 不知道根因就开始猜测性修补，你看不懂代码无法判断修改是否合理，只能说"还是不对"，然后 AI 再猜一次 |
| 对话上下文漂移 | 长对话里前面确认过的边界后面会被忘掉，AI 可能把之前定好的规则改掉了你也不知道 |

### 六个常见失败模式

| 失败模式 | 表现 | 根因 |
|---------|------|------|
| 贪多嚼不烂 | 一次性描述十几个功能，AI 全做完后一堆问题 | 没有控制 MVP 范围 |
| 对话漂移 | 聊了几十轮后，AI 忘了之前定的规则 | 没有把关键约束写成文件 |
| 修 bug 螺旋 | 改 A 坏 B，改 B 坏 C，越改越乱 | 没有限制单次改动范围，没有分步验证 |
| 黑盒信任 | AI 说"改好了"，你直接信了，没验证 | 没有可操作的验证步骤 |
| 技术债积累 | 项目越做越慢，加一个小功能要改很多地方 | AI 早期做了不好的架构决策，你无法识别 |
| 推翻重来 | 项目做到一半推翻重做 | 没有阶段性保存，也没有分层实现 |

---

## 第三部分：正确的全流程

### 从零做一个产品的完整路径

```
阶段 1：想法 → 产品定义
阶段 2：产品定义 → 技术选型（AI 主导，你确认）
阶段 3：技术选型 → 项目初始化（确认能跑起来）
阶段 4：逐个功能实现（分步验证）
阶段 5：修 bug 和调整（有协议约束）
阶段 6：保存和备份（阶段性提交）
```

### 阶段 1：想法变产品定义

**你的输入：** 一段话，描述你想做什么。

例如："我想做一个小工具，帮我管理客户跟进记录，能记录每次沟通内容，提醒我该跟进谁了。"

**AI 应该帮你做的事：**
- 反问你：谁用？用在哪里？手机还是电脑？需要多人用还是你一个人用？数据存在哪里？
- 输出一份简短的产品定义
- 列出核心功能（不超过 5 个），每个功能一句话
- 明确第一个版本只做什么（MVP 边界）

**关键原则：第一个版本只做 3 个核心功能，不要贪多。**

但注意：**产品定义要完整，实现范围要收窄。** 在阶段 1 列出你能想到的所有功能（比如 15 个），然后标出哪 3 个是第一期做的。这样 AI 在下一步技术选型时看到的是完整全貌，不会因为只看 3 个功能而选错技术方案。

### 阶段 2：技术选型

你不需要懂技术，但你需要告诉 AI 几个约束：
- 你用什么电脑（Windows / Mac）
- 你希望这个东西跑在哪里（本地 / 网页 / 手机）
- 你有没有服务器（没有就用本地或免费服务）
- 你希望数据存在哪里（本地文件 / 云端数据库）

AI 根据这些约束和阶段 1 的完整功能列表来选技术栈，然后用大白话告诉你"我打算用 XX 来做，因为 YY，你同意吗"。

**关键原则：让 AI 把技术选型写进一个文件里，永久留存。** 技术选型基于完整产品全貌，不是只看第一期 MVP。

### 阶段 3：项目初始化

AI 帮你创建项目结构、安装依赖、生成基础代码。

你需要做一件事：**确认项目能跑起来。**

AI 应该给你一个明确的启动命令（比如"在终端里输入 `npm start`，然后打开浏览器访问 `http://localhost:3000`"），你照做，能看到一个空白页面或欢迎页面，就说明成功。

**关键原则：在写任何功能代码之前，先确认空项目能跑。**

### 阶段 4：逐个功能实现

这是最长的阶段。对每个功能，推荐走这个循环：

```
你描述功能（自然语言）
    ↓
AI 先澄清需求边界
    ↓
AI 输出行为契约（你逐条确认）
    ↓
AI 输出技术设计（你确认准备怎么做）
    ↓
AI 先写测试或先定义验证标准
    ↓
AI 小步实现（一次只改少量文件）
    ↓
AI 先自证：跑测试 / 给你明确验证步骤
    ↓
正式检查是否真的完成
    ↓
通过后保存进度 → 下一步
```

如果映射到这套模板里的技能和 Agent，大致是：

- `behavior-contract`：把模糊需求变成可执行的行为契约
- `planning-agent` / `docs/design/`：把“做什么”变成“准备怎么做”
- `using-git-worktrees`：新功能、复杂修复、高风险改动时，优先把实现放到隔离工作区
- `test-driven-development`：先写失败测试，或至少先写清验证方式
- `guided-implementation` / `dev-agent`：按设计小步实现
- `verification-before-completion`：在说“做完了”前，先拿到新鲜证据
- `requesting-code-review`：做正式质量关卡，判断能不能继续
- `finishing-a-development-branch` / `release-agent`：明确是继续开发、等待确认、可提交还是可发布

**关键原则：一个功能完全做完、验证通过、审查通过后，再开始下一个。不要并行。**

这比“AI 改完你大概试试”更稳，因为每一段都有明确的停点和放行条件。 / This is more reliable than “let the AI change it and you try it roughly,” because each stage has a clear stop point and go/no-go check.

### PRD 和行为契约的关系

PRD 回答"这个功能是什么、为什么做、谁用、大概怎么用"。
行为契约回答"用户点这个按钮时，系统具体做什么、返回什么、出错怎么办"。

**示例对比：**

PRD 里写：
> 用户可以通过手机号注册账号。

行为契约里写：
> - 用户输入手机号，点击"获取验证码"
> - 手机号为空时，提示"请输入手机号"
> - 手机号格式不对时，提示"手机号格式错误"
> - 手机号已注册时，提示"该手机号已注册，请直接登录"
> - 验证码 60 秒内不能重复发送
> - 验证码输入错误时，提示"验证码错误"，可重试 3 次
> - 3 次错误后锁定 10 分钟

PRD 给方向，行为契约给边界。AI 拿着 PRD 会自由发挥，拿着行为契约只能照做。

**两者都要，不是二选一。**

### 阶段 5：修 bug

正确的做法：

1. 你描述现象（"我点了 XX 按钮，出现了 YY，但我期望看到 ZZ"）
2. AI 先用大白话解释为什么会出这个问题
3. 你确认这个解释是否符合你看到的情况
4. AI 提出最小修改方案，你同意后才改
5. 改完立刻验证

**关键原则：连续修 3 次还没解决，必须停下来。** 这时候应该：
- 让 AI 重新从头分析这个问题
- 或者回退到上一个正常状态，重新来过
- 或者开一个新对话，把问题重新描述一遍

### 阶段 6：保存和备份

每次一个功能做完且验证通过后，让 AI 帮你用 Git 提交一次。

这样当后面改坏了，你可以说"帮我回到上次保存的版本"。

---

## 第四部分：AI 协作脚手架

### 什么是脚手架

你要搭建的东西专业叫法是 **项目级 AI 协作脚手架**（Project AI Scaffold）。它不是工具（工具是可执行程序），不是配置（配置只是参数），也不是项目初始化（初始化只做一次）。它是一套持续生效的工作流约束。

### 脚手架包含什么

针对不懂代码的人，建议搭建 7 个资产，分 3 层：

#### 第一层：全局约束（始终生效）

**1. 项目级说明文件**

不同工具有不同的项目说明文件，相当于给 AI 的"长期工作规则"：
- Claude Code 读 `CLAUDE.md`（项目根目录）
- GitHub Copilot 读 `.github/copilot-instructions.md`（`.github/` 目录下）
- Codex 读 `AGENTS.md`（任意目录，可多层嵌套）

建议写：
- 项目是什么、技术栈、目录结构、常用命令
- 硬规则：任何功能实现前，必须先有行为契约
- 硬规则：能先写测试就先写测试；不适合先写测试也必须先定义验证方式
- 硬规则：单次改动不超过 3 个文件，超过必须拆分
- 硬规则：修 bug 时，先用大白话解释根因，你确认后才改
- 硬规则：说"完成 / 修复 / 可提交 / 可发布"前，必须先验证
- 硬规则：实现完成后，还要再过一次代码审查

对这套模板来说，`CLAUDE.md`（CC）、`copilot-instructions.md`（GC）、`AGENTS.md`（Codex）分别是三个工具的主入口。如果你同时用多个工具，把它们各自的配置文件都部署上即可。

#### 第二层：关键流程（按需触发）

**2. 需求澄清入口 `clarify-my-requirement`**

你有新想法时的固定入口。AI 先反问，再帮你把模糊想法收敛成产品定义和行为契约。

**3. 行为契约技能 `behavior-contract`**

核心技能。把“我想要什么”变成“点哪里会发生什么、出错时提示什么”的具体规则。

**4. 技术设计 / 计划阶段 `planning-agent` + `docs/design/`**

行为契约确认后，再把“准备怎么做”写清楚，避免 AI 直接跳进代码自由发挥。

**5. 隔离工作区技能 `using-git-worktrees`**

在新功能、复杂修复、高风险改动开始前，优先把实现放到独立工作区，先保护当前主线，再开始动代码。

**6. 测试先行技能 `test-driven-development`**

先写失败测试，或者至少先写清验证标准。这样后面不是靠感觉说“应该好了”。

**7. 分步实现技能 `guided-implementation` / `dev-agent`**

实际写代码的流程。基于行为契约和设计分步改文件，每步都要能解释、能验证、能回退。

**8. 修 bug 入口 `diagnose-and-fix` + `systematic-debugging`**

遇到 bug 时，先查文档和现象，先解释根因，再做最小修改。不是上来就猜着改。

**9. 完成前验证 `verification-before-completion`**

这是防止“AI 说改好了，其实没证据”的关键一步。必须先有新鲜的测试结果、手动验证结果，才能说做完。

**10. 正式审查 `requesting-code-review`**

这是放行关卡。它和 `code-review` 不一样：
- `code-review` 是大白话解释型审查
- `requesting-code-review` 是正式判断“能不能继续”的质量关卡

**11. 收尾 `finishing-a-development-branch` / `release-agent`**

用来明确当前状态：继续开发、等待确认、可提交、可发布。这样每次结束都知道下一步是什么。

#### 第三层：文档沉淀（长期有效）

**12. 行为契约存档 `docs/contracts/`**

每个功能的正确行为写在这里。AI 修 bug、写测试、做审查前都应该先看它。

**13. 技术设计存档 `docs/design/`**

每个功能准备怎么做、改哪些文件、怎么验证，写在这里。这样 AI 不会每次都临场发挥。

**14. 计划与执行记录 `docs/plans/`**

用来放批准后的计划和执行日志，方便回看每一步做了什么。

**15. 验证清单存档 `docs/verification/`**

每个功能的验证步骤和验证结果存在这里。出问题时先跑已有清单，定位更快。'},

### 日常使用方式

**做新功能时：**
1. 在 Copilot 聊天里使用需求澄清入口
2. 用你自己的话说"我想加一个 XX 功能"
3. AI 反问你问题，帮你理清边界
4. AI 输出 PRD，你确认
5. AI 细化为行为契约，你逐条确认
6. AI 分步实现，每步你验证
7. 全部通过后保存进度

**发现 bug 时：**
1. 使用修 bug 入口
2. 描述你看到的现象
3. AI 用大白话解释原因
4. 你确认后 AI 最小修改
5. 改完你验证

---

## 第五部分：AI 环境配置指南

### 三种主流环境对比

不同的 AI 工具读取不同的配置文件，把你的规则放在对应工具能识别的位置才会生效。

| 对比项 | GitHub Copilot | Claude Code | **Codex** |
|--------|---------------|-------------|-------------|
| 专属配置文件 | `.github/copilot-instructions.md` | `CLAUDE.md` | **`AGENTS.md`** |
| 文件位置 | 必须在 `.github/` 下 | 项目根，可多层 | **任意目录，可多层嵌套** |
| 多层叠加 | 不支持 | 支持 | **支持，深目录覆盖浅目录** |
| 子 Agent | `.github/agents/` 文件定义 | `.claude/agents/` 文件定义 | **`codex/agents/` 预设 + spawn_agent 调用** |
| 技能/Skills | `.github/skills/` 项目级 | `.claude/skills/` 项目级 | **全局安装，不在项目目录内** |
| 生效触发 | 打开 VS Code 自动生效 | 运行 `claude` 自动生效 | **进入项目目录自动生效** |
| 内容定位 | 行为规则 + 项目约束 | 静态事实 + 命令 + 规则 | **分层规则：根管全局，子目录管细节** |### GitHub Copilot 初始化配置

**第一步：创建配置文件**

```
your-project/
└── .github/
    └── copilot-instructions.md   ← 必须在这个路径
```

**第二步：`.github/copilot-instructions.md` 应该写什么**

```
## 项目说明
- 项目是什么：一句话描述
- 技术栈：（AI 选型后填写）
- 目录结构：src/ 是源码，docs/contracts/ 是行为契约

## 构建和启动命令
- 启动：npm start
- 测试：npm test

## 硬规则
1. 任何功能实现前，必须先输出行为契约，我确认后才能动代码
2. 单次改动不超过 3 个文件，超过必须拆分步骤
3. 每次改动后，必须提供具体的界面操作验证步骤
4. 修 bug 时，先用大白话解释根因，我确认后才改
5. 不允许在我没确认的情况下重构、重命名、或改动不相关文件
```

**第三步：打开 VS Code，Copilot 自动读取，无需额外操作。**

### Claude Code 初始化配置

**第一步：创建根目录配置文件**

```
your-project/
├── CLAUDE.md          ← 项目全局规则
└── src/
    └── CLAUDE.md      ← 可选，src 模块局部规则（会叠加）
```

**第二步：根目录 `CLAUDE.md` 应该写什么**

```
## 项目说明
- 项目是什么：一句话描述
- 技术栈：列出框架和语言
- 目录结构：src/ 是源码，docs/contracts/ 是行为契约

## 常用命令
- 启动：npm start
- 测试：npm test
- 安装依赖：npm install

## 约束
- 所有功能实现前必须有已确认的行为契约
- 能先写测试就先写测试；否则先写清验证方式
- 单次改动不超过 3 个文件
- 修 bug 时先解释根因，确认后才改
- 声称完成前必须先验证
- 实现完成后必须再过一次代码审查
```

**第三步：在项目目录下运行 `claude` 命令，CC 自动读取所有层级的 `CLAUDE.md`。**

**第四步：把技能和文档目录接上流程**

至少准备这些目录：
- `.claude/skills/`
- `.claude/agents/`
- `docs/contracts/`
- `docs/design/`
- `docs/plans/`
- `docs/verification/`

这样 Claude Code 才不只是“知道规则”，而是真的有地方沉淀契约、设计、计划和验证证据。


### Codex 初始化配置

**第一步：部署多层 AGENTS.md**

```
your-project/
├── AGENTS.md                       ← 项目全局规则
├── src/
│   └── AGENTS.md                   ← 可选，代码规范
├── docs/
│   ├── contracts/
│   │   └── AGENTS.md               ← 可选，契约规范
│   ├── verification/
│   │   └── AGENTS.md               ← 可选，验证规范
│   ├── design/
│   │   └── AGENTS.md               ← 可选，设计规范
│   └── plans/
│       └── AGENTS.md               ← 可选，计划+日志规范
└── tests/
    └── AGENTS.md                   ← 可选，测试规范
```

**第二步：根目录 `AGENTS.md` 应该写什么**

```
## 项目说明
- 项目是什么：一句话描述
- 技术栈：列出框架和语言
- 目录结构：src/ 是源码，docs/contracts/ 是行为契约

## 常用命令
- 启动：npm start
- 测试：npm test

## 约束
- 所有功能实现前必须有已确认的行为契约
- 能先写测试就先写测试；否则先写清验证方式
- 单次改动不超过 3 个文件
- 修 bug 时先解释根因，确认后才改
```

**第三步：进入项目目录，运行 `codex` 命令，Codex 自动读取所有层级的 `AGENTS.md`。**

**第四步：把 Agent 角色文件部署到位**

将 `codex/agents/` 目录复制到项目根：

- `codex/agents/product-agent.md` — 产品 Agent 人设
- `codex/agents/syseng-agent.md` — 架构 Agent 人设
- `codex/agents/planning-agent.md` — 规划 Agent 人设
- `codex/agents/dev-agent.md` — 开发 Agent 人设
- `codex/agents/test-agent.md` — 测试 Agent 人设
- `codex/agents/release-agent.md` — 发布 Agent 人设

这样 Codex 在调用子 Agent 时，会按 AGENTS.md 中的调度规则自动读取对应角色文件。
### 三个环境都需要的通用文件

```
your-project/
├── .github/
│   ├── copilot-instructions.md    # Copilot 行为规则（必须）
│   └── prompts/
│       └── *.prompt.md            # 可复用提示词入口（可选）
├── CLAUDE.md                      # CC 静态事实（CC 用户必须）
├── AGENTS.md                      # Codex 全局规则（Codex 用户必须）
├── src/
│   └── AGENTS.md                  # 代码规范（Codex 可选）
├── docs/
│   ├── contracts/
│   │   └── AGENTS.md              # 契约规范（Codex 可选）
│   ├── verification/
│   │   └── AGENTS.md              # 验证规范（Codex 可选）
│   ├── design/
│   │   └── AGENTS.md              # 设计文档规范（Codex 可选）
│   └── plans/
│       └── AGENTS.md              # 计划+日志规范（Codex 可选）
├── tests/
│   └── AGENTS.md                  # 测试规范（Codex 可选）
├── .editorconfig                  # 缩进、换行规范（可选但推荐）
└── .gitignore                     # 忽略不该被 AI 操作的文件
```

### 已有项目如何接入

如果你的项目已经存在，不需要重建，按以下步骤补充配置即可：

1. 创建 `.github/copilot-instructions.md`，填入项目现有的技术栈和目录结构
2. 如果用 CC，在项目根创建 `CLAUDE.md`，内容参照上面模板
3. 如果用 Codex，在项目根创建 `AGENTS.md`，其余分层 AGENTS.md 按需放入对应子目录
4. 创建 `docs/contracts/` 目录，从下一个功能开始沉淀行为契约
5. 不需要动现有代码，配置文件只影响 AI 的行为，不影响程序运行

---

## 第六部分：关于这个模板仓库

### 它是什么

这是一个 **AI 代理协作开发模板仓库**，不是一个可运行的业务应用。它打包了一套可复用的工作流约束（prompts、skills、agents、templates），让你把 GitHub Copilot / Claude Code / Codex CLI 按阶段化流程接入自己的项目。

### 它的价值

| 价值点 | 说明 |
|-------|------|
| 阶段化流程 | 把 AI 协作拆成 12 个阶段，避免跳步和无约束乱改 |
| 资产分层 | prompt（入口）、skill（流程）、agent（子代理）、template（模板）各司其职 |
| 文档即契约 | 用活跃文档集给 AI 建立可追溯的长期记忆 |
| 执行纪律 | 内置验证、root-cause debugging、one slice per commit 等工程约束 |
| 可落地 | 提供环境安装脚本，不只是概念框架 |

### 它的局限

| 局限 | 说明 |
|------|------|
| 上手成本 | 更适合有工程成熟度的团队，不适合只想快速生成代码的人 |
| 二次定制 | 必须改写 instructions 和静态事实文件才能在你的项目里生效 |
| 对小项目偏重 | 完整 12 阶段对轻量项目可能过度正式 |
| 依赖自律 | 仓库提供结构但不能强制团队遵守流程 |

### 适用场景

**高价值场景：**
- 想把 AI 纳入团队正式交付流程
- 项目需要长期设计文档和可追溯决策
- 需要多个代理角色协同
- 需要可复制到多个项目的统一方法

**低价值场景：**
- 只想快速起一个 demo
- 没有文档习惯
- 项目规模太小，流程成本高于协作收益

---

## 第七部分：关键原则速查

| 原则 | 说明 |
|------|------|
| 第一版只做 3 个核心功能 | 贪多是 Vibe Coding 失败的第一原因。但产品定义要完整，技术选型看全貌 |
| 关键约束写成文件 | 不靠对话记忆，对话会漂移 |
| 一次只做一个功能 | 做完验证、审查通过后再开始下一个 |
| 分步实现分步验证 | 一次只改少量文件，每步都验证 |
| 先契约，后设计，再实现 | 不先把“应该怎样”和“准备怎么做”写清楚，就不要直接动代码 |
| 能测试先测试 | 能先写测试就先写测试；不适合也要先定义验证方式 |
| 修 bug 先解释后动手 | AI 必须先说清楚为什么，你确认后才改 |
| 3 次改不好就停 | 连续修 3 次未解决，必须换策略 |
| 完成前必须先验证 | 没有新鲜证据，不要接受“已经做好了” |
| 审查分两类 | `code-review` 负责解释，`requesting-code-review` 负责放行判断 |
| 交付状态要说清楚 | 每次结束都要明确：继续开发 / 等待确认 / 可提交 / 可发布 |
| 阶段性保存 | 每个功能做完用 Git 提交一次 |
| AI 选的技术方案要留档 | 方便以后出问题时快速定位 |
| 行为契约才是实现依据 | PRD 给方向，行为契约给边界 |
| 空项目先确认能跑 | 再写功能代码之前确保基础没问题 |
